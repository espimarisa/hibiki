version: "3.9"
services:
  hibiki:
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hibiki
    depends_on:
      - caddy
    restart: unless-stopped

volumes:
  hibiki:
