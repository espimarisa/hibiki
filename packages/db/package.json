{
  "name": "@espimarisa/hibiki-db",
  "version": "7.0.0-alpha",
  "type": "module",
  "description": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/espimarisa/hibiki.git"
  },
  "homepage": "https://github.com/espimarisa/hibiki#readme",
  "bugs": {
    "url": "https://github.com/espimarisa/hibiki/issues"
  },
  "author": "Espi Marisa <contact@espi.me> (https://espi.me)",
  "license": "zlib",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "db:generate": "bun drizzle-kit generate:pg",
    "db:push": "bun drizzle-kit push:pg",
    "format": "prettier --write .",
    "h": "bun run ./src/test.ts",
    "lint": "tsc --noEmit && prettier --c . && eslint .",
    "studio": "bun run db:generate && drizzle-kit studio",
    "test": "bun run lint"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.1",
    "postgres": "^3.4.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.14"
  }
}
